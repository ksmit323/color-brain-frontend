[application]

[web.app]

# HTML title tag content
title = "Color Brain — Dye Recipe Intelligence"

[web.watcher]

# Hash links (e.g. /#contact) should serve the SPA shell, not 404.
index_on_404 = true

# include `assets` in web platform
[web.resource]

# Additional CSS style files
style = []

# reveal.js is loaded via document::Script + asset! in main.rs (bundled with other assets)
script = []

[web.resource.dev]

# Javascript code file
# serve: [dev-server] only
script = []
